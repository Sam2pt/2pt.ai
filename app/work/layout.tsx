import React from "react"

/**
 * Pass-through layout. All work metadata is set on the individual pages
 * so it stays in sync with the case data source.
 */
export default function WorkLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
